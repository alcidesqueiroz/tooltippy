{
  "name": "tooltippy",
  "version": "1.0.0",
  "description": "💬 Graceful CSS-only tooltips in <1kb.",
  "license": "MIT",
  "repository": "alcidesqueiroz/tooltippy",
  "author": {
    "name": "Alcides Queiroz",
    "email": "alcidesqueiroz@gmail.com",
    "url": "alcidesqueiroz.github.com"
  },
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build-expanded": "stylus src/themes/${THEME}/index.styl --out dist/tooltippy--${THEME}.css",
    "build-debug": "stylus -w src/themes/${THEME}/index.styl --out dist/tooltippy--${THEME}.debug.css --sourcemap",
    "build-min": "stylus src/themes/${THEME}/index.styl --out dist/tooltippy--${THEME}.min.css --compress",
    "prefixify-expanded": "postcss dist/tooltippy--${THEME}.css -u autoprefixer -r",
    "prefixify-debug": "postcss dist/tooltippy--${THEME}.debug.css -u autoprefixer -r",
    "prefixify-min": "postcss dist/tooltippy--${THEME}.min.css -u autoprefixer -r",
    "build-all": "npm run build-expanded && npm run build-debug && npm run build-min",
    "postbuild-all": "npm run prefixify-expanded && npm run prefixify-debug && npm run prefixify-min",
    "prebuild": "npm run clean && mkdir dist",
    "build": "for z in biz high-contrast hipster translucid; do (export THEME=\"${z}\" && npm run build-all); done",
    "test": "test/test-build"
  },
  "keywords": [
    "tooltips",
    "hints",
    "CSS-only",
    "graceful",
    "tiny",
    "micro"
  ],
  "devDependencies": {
    "autoprefixer": "^8.1.0",
    "postcss-cli": "^5.0.0",
    "stylus": "^0.54.5"
  }
}
